Lumen
=====

Lumen is an application for the production of musically coordinated
christmas light displays.

Requirements
-------------
 * Pyparallel (python-parallel on Debian systems).
 * Pygame (python-pygame on Debian systems).

Notes
------
 * On Linux systems, Pyparallel requires that the ppdev kernel module
   be loaded (be sure to unload lp!)
